# 更新日志

本项目的所有重要变更都会记录在此文件中。版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.1.0] - 2026-08-06

首个公开发布版本。

### 新增

- **纵向标签列表** — 侧边栏展示所有标签，支持键盘导航、重复标签合并、就地静音、专注模式，`Cmd/Ctrl + K` 搜索
- **AI 智能分组** — 接入 OpenAI 兼容端点按语义归类，规则先行 + 增量分组 + 本地兜底
- **来源链路** — 按 `openerTabId` 把标签串成树，支持折叠和整条链路关闭
- **AI 标签助手** — 对话式问答与操作，可读取页面正文、整页截图、检索历史与书签；改动类操作先预览再确认
- **归档抽屉** — 关闭前先归档，可搜索、批量恢复、导出 Markdown，支持按闲置天数自动归档
- **刚关闭与会话管理** — 基于 `chrome.sessions` 找回误关标签，保存与恢复整套标签布局
- **智能清理与标签休眠** — AI 判断可清理项（默认归档而非关闭），长时间未活动的标签自动休眠
- **性能页** — 按需注入探针读取 JS 堆、DOM 节点、资源体积等指标，不扫描时零常驻开销
- **设置** — 深色 / 浅色主题、AI 端点与密钥配置、配置备份与恢复，数据全部保存在本地
