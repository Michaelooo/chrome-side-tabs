# Side Tabs

Chrome 侧边栏标签管理扩展，基于 Manifest V3 + Side Panel API，内置可对话的 AI 标签助手。

介绍网站：https://michaelooo.github.io/chrome-side-tabs/

## 功能

### 两种形态

Chrome 145+ 有了原生垂直标签栏（右键标签栏 → 以垂直方式显示标签页）。它把纵向列表、标签组的展示与折叠都做了，和本扩展的侧栏重合。所以设置页提供两种形态，按你用不用原生垂直标签栏来选：

| | **侧边栏**（默认） | **页面浮球** |
|---|---|---|
| 适用 | 传统水平标签栏 | 已开启原生垂直标签栏 |
| 标签列表 | 扩展侧栏里 | 交给 Chrome 原生标签栏 |
| 入口 | 工具栏图标开侧栏 | 快捷键 / 工具栏徽标 / 左边缘把手 |
| 能力 | 下面全部 | AI 整理分组、智能清理、归档抽屉、AI 助手 |

浮球模式下 AI 分组的结果直接同步进 Chrome 原生标签组，也就是说**在浏览器自己的垂直标签栏里就能看到整理结果**，扩展不再画第二份列表。

#### 浮球模式的三个入口

面板不常驻，靠召唤。常驻挂件有个死结：低调到不烦人，就一定低调到看不见。

| 入口 | 管什么 |
|---|---|
| 快捷键 | 主入口。任意页面按下，面板居中弹出。Windows/Linux 默认 `Alt+Shift+T`，macOS 默认 `⌘⇧E`，可在 `chrome://extensions/shortcuts` 改键（设置页显示的是实际绑定值，不是写死的） |
| 工具栏图标徽标 | 通知。未分组标签攒到 8 个以上，扩展图标上出现数字角标，点图标从左边滑出面板。需要图标已固定在工具栏 |
| 左边缘把手 | 兜底。视口左边缘中部一块 26px 深色片，划过去变宽 |

面板是**浮动卡片**，不是第二根侧栏：不贴边（留 34px 缝）、不全高（520px 封顶）、`position: fixed` 不推布局。你已经开着原生垂直标签栏了，再来一根贴边全高的栏就是横向被吃两次。

Chrome 没有提供检测原生垂直标签栏是否开启的 API，所以形态只能手动选，不会自动切换。

### 标签列表

- **纵向标签列表** — 在浏览器侧边栏显示所有标签，替代顶部水平标签栏
- **AI 智能分组** — 接入 OpenAI 兼容端点（DeepSeek、OpenAI 等），按语义自动归类
  - 规则先行：自定义规则命中的标签直接归组，完全不发给 AI，省 token 且结果确定
  - 增量分组：只把没见过的标签送给 AI，已有判断从缓存复用（按住 Shift 点击可忽略缓存重算）
  - 本地兜底：AI 不可用时按「主域名 + 标题词重叠」聚类，比纯域名分组更准
- **来源链路** — 记录 `openerTabId`，按「这个标签是从哪儿点开的」串成树，支持折叠和整条链路关闭
- **键盘导航** — `j`/`k` 上下移动、`x` 关闭、`Enter` 跳转，输入框内不接管
- **重复标签合并** — 同 URL 的标签显示 `×N` 角标，点击合并
- **就地静音** — 有声音的标签直接在列表里静音，不用先跳过去找
- **专注模式** — 锁定一个分组，其余标签自动折叠并休眠
- **搜索** — 侧边栏内 `Cmd/Ctrl + K` 唤起

### AI 标签助手

侧栏右下角浮球，点开是对话浮层。它能看到所有窗口标签的标题、域名、闲置时长、分组归属、重复情况和来源链路。

- **问答 + 操作一体** — 问「哪些标签在摸鱼」会列出判断和理由并高亮，底下跟操作按钮
- **读页面正文** — 「总结一下这个页面」「这几个文档讲的是一回事吗」，读的是真实内容不是标题
- **可执行的操作** — 归档 / 关闭 / 休眠 / 重载 / 固定 / 静音 / 分组 / 跳转 / 移到新窗口 / 打开网页 / 导出书签 / 恢复关闭的标签
- **整页截图** — 自动识别真正在滚动的元素（含 SPA 的内部滚动容器），逐屏滚动拼接成长图，超出画布上限自动降采样
- **分析标签占用** — 问「哪些标签最吃内存」「浏览器为什么这么卡」，它会实测各标签的 JS 堆、DOM 节点、图片位图，再给出该关谁的建议。5 分钟内扫过的直接复用结果，追问不会反复注入探针
- **检索历史与书签** — 需要时自动申请权限，查完再回答
- **对话历史** — 自动保存，可新建对话、加载历史继续聊；自动清理最近 20 个 / 30 天内 / 每个会话 60 条
- **安全边界** — 只读操作直接执行，会改动标签的操作一律先预览再等确认，关闭类标红

### 归档与恢复

- **归档抽屉** — 关掉但不丢。标签右键、分组标题、智能清理、性能页都能归档，可搜索、批量恢复、导出 Markdown
- **自动归档** — 超过设定天数未访问的标签自动收进归档并关闭
- **刚关闭** — 基于 `chrome.sessions` 找回误关的标签（归档管有意关的，这里管手滑关的）
- **会话管理** — 保存/恢复整套标签布局
- **智能清理** — AI 判断哪些标签可以清理，默认动作是归档而非关闭
- **标签休眠** — 自动休眠长时间未活动的标签，释放内存

### 性能页

独立页面（侧栏底部闪电图标进入），按需扫描，**不扫描时常驻开销为 0**。

- 一次性注入探针读取 JS 堆、DOM 节点数、图片解码位图、资源体积、iframe 数
- 首屏展示探针自身开销（平均每标签占用主线程多少毫秒），代价摊开给你看
- 重量分与浪费分排序，按分组、按共用渲染进程的站点分别聚合
- 注意力账单：打开后再没回访过的标签数、平均闲置时长、标签黑洞站点
- 已休眠标签自动跳过，避免注入把它唤醒
- **让 AI 分析** — 扫完点一下，把这份报告交给助手，让它给出该关掉/休眠哪些最划算的建议

### 其他

- 深色 / 浅色主题
- 配置备份与恢复（导出 JSON，API Key 可选是否包含；导入采用合并策略，不冲掉现有数据）

## 权限说明

**安装即需要**：

| 权限 | 用途 |
|---|---|
| `tabs` `tabGroups` | 读取和操作标签、同步原生标签组 |
| `storage` | 本地保存设置、归档、对话历史 |
| `sidePanel` `alarms` `favicon` | 侧边栏、定时任务、图标 |
| `scripting` + `<all_urls>` | 性能探针、整页截图、读取页面正文、页面浮球 |

页面浮球是一段内容脚本。它在所有页面加载，但**侧边栏形态下读一次设置就立即退场**，不碰页面内容、不注入任何界面。只有切到浮球形态才会挂出浮球，且浮球本身也只负责画一个按钮和一个 iframe，实际逻辑跑在 iframe 里的扩展页中。

**可选权限**（安装时不索取，用到才申请，可随时在设置页收回）：

| 权限 | 用途 |
|---|---|
| `history` | 在浏览历史里检索 |
| `bookmarks` | 检索书签、导出标签为书签文件夹 |
| `sessions` | 查看和恢复最近关闭的标签 |

所有数据仅存在浏览器本地，API Key 存于 `chrome.storage.local`，不会上传到除你自己配置的 AI 端点之外的任何地方。

## 技术栈

- Vite + React 19 + TypeScript
- Tailwind CSS v4
- CRXJS Vite Plugin（Chrome Extension HMR）
- Chrome Manifest V3 + Side Panel API

## 开发

```bash
# 安装依赖
pnpm install

# 开发模式（支持 HMR）
pnpm dev

# 构建
pnpm build
```

### 加载扩展

1. 打开 `chrome://extensions`
2. 开启"开发者模式"
3. 点击"加载已解压的扩展程序"，选择 `dist` 目录

### 首次配置

1. 点击扩展图标打开侧边栏
2. 点击"AI 分组"按钮或进入扩展选项页
3. 填写 API 地址（如 `https://api.deepseek.com`）、API Key、模型名称
4. 可在选项页查看系统提示词、添加自定义分组规则、配置自动归档

### 侧边栏位置

前往 `chrome://settings/appearance`，在"侧边栏"选项中选择"在左侧显示"。

## 项目结构

```
src/
├── background/              # Service Worker
│   ├── service-worker.ts    # 生命周期、形态对齐、工具栏图标、whoami
│   ├── tab-watcher.ts       # 标签事件监听 + 来源链路记录
│   ├── suspend-manager.ts   # 自动休眠
│   └── stash-manager.ts     # 自动归档
├── content/
│   └── orb.ts               # 页面左下角浮球（Shadow DOM + iframe 宿主）
├── sidepanel/               # 侧边栏形态
│   ├── App.tsx              # 标签列表、分组、来源链路、专注模式
│   ├── Assistant.tsx        # AI 助手（侧栏浮层 / 浮球面板内嵌两用）
│   └── Markdown.tsx         # 轻量 Markdown 渲染
├── orb/                     # 浮球形态
│   └── ToolsPanel.tsx       # AI 整理 / 清理 / 归档 / 助手，无标签列表
├── panels/                  # 两种形态共用的浮层
│   ├── CleanupOverlay.tsx
│   └── StashDrawer.tsx
├── perf/                    # 性能页（独立页面）
│   └── PerfApp.tsx
├── options/                 # 设置页
│   └── OptionsApp.tsx
├── lib/
│   ├── ai-client.ts         # 分组编排：规则 → 缓存 → AI → 本地聚类
│   ├── tab-tools.ts         # 两种形态共用：整理、建组、清理候选、分组重连
│   ├── mode.ts              # 形态切换与生效
│   ├── perf-store.ts        # 扫描结果会话缓存 + 压给模型的紧凑格式
│   ├── assistant.ts         # 助手大脑：工具定义、上下文构建、响应解析
│   ├── tab-actions.ts       # 助手操作执行：截图、书签、恢复等
│   ├── page-reader.ts       # 页面正文提取
│   ├── perf-probe.ts        # 性能探针与评分
│   ├── provenance.ts        # 来源链路
│   ├── stash.ts             # 归档
│   ├── chat-store.ts        # 对话历史
│   ├── recently-closed.ts   # 最近关闭
│   ├── permissions.ts       # 可选权限管理
│   ├── backup.ts            # 配置备份/恢复
│   ├── storage.ts
│   └── tab-manager.ts
├── types/
└── styles/globals.css
```

侧边栏和浮球面板都是 extension page，可以直接调用 `chrome.tabs`，不经 service worker 中转。
浮球面板是内容脚本挂进页面的 iframe——**因为它是扩展页，所以业务逻辑原样复用，不需要把一切塞进消息里转发**。
后台只做它独有的事：生命周期初始化、标签事件监听、自动休眠、自动归档，加上告诉浮球它在哪个窗口（`sender.tab.windowId`）和接管浮球形态下的工具栏图标点击。

## 已知限制

- **形态切换**：Chrome 不提供检测原生垂直标签栏是否开启的 API，只能手动选
- **把手覆盖范围**：`chrome://` 页、Chrome 应用商店、PDF 阅读器、其他扩展页上浏览器禁止注入内容脚本，把手不会出现。这些页面按快捷键或点工具栏图标会打开独立面板作为兜底
- **把手位置**：固定在视口左边缘中部，不可拖动。页面自己若有左侧吸边元素可能重叠
- **分屏**：Chrome 未开放创建拆分视图的 API，扩展只能读取分屏状态。助手被问到时会给出手动操作说明
- **整页截图**：虚拟滚动列表（只渲染可视区域的信息流）无法截全；最多 20 屏
- **来源链路**：只对本次浏览器会话内新开的标签生效，`openerTabId` 仅在标签创建时可得
- **对话历史**：截图不入历史（dataURL 体积过大，会撑爆存储配额）

## 许可

MIT
